mutation updateRelatorio(
  $id: ID!,
  $name: String!,
  $status: String!,
  $revDeadline: String!,
  $delDeadline: String!
){
  updateRelatorio(
    id: $id,
    name: $name,
    status: $status,
    revDeadline: $revDeadline,
    delDeadline: $delDeadline
  )

}
